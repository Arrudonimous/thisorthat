export default function Button() {
  return (
    <button
      type="submit"
      className="font-bold py-3 w-full text-lg"
      // onClick={handleNavigate}
    >
      Entrar

    </button>
  );
}
